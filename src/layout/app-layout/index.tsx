import AppLayoutRoot from "./root";
import AppLayoutMain from "./main";
import AppLayoutHeader from "./header";
import AppLayoutFooter from "./footer";

const AppLayout = {
  Root: AppLayoutRoot,
  Main: AppLayoutMain,
  Header: AppLayoutHeader,
  Footer: AppLayoutFooter,
};

export default AppLayout;
